# ช่องทางรีวิวร่วม — Codex ↔ Claude

ไฟล์นี้คือ **ช่องทางเดียวที่ตัวรีวิวกับตัวแก้คุยกันได้** — ทั้งสองตัวไม่มีทางส่งข้อความถึงกันโดยตรง
แต่เปิดโฟลเดอร์เดียวกัน (`C:\Users\User\Projects\import-export-os`) จึงใช้ไฟล์ใน repo เป็นความจำร่วม

> ตรงกับหลักที่บันทึกไว้ใน `CLAUDE.md` หัวข้อ loop/graph engineering: **"the agent forgets, the graph does not"**
> — บทสนทนาหายไปทุกรอบ แต่ artifact ใน repo อยู่ต่อ

---

## บทบาท

| | Codex | Claude |
|---|---|---|
| แก้ไฟล์ | ได้ — **แต่บน branch ของตัวเองเท่านั้น** | บน `main` (และรีวิว branch ของ Codex) |
| หน้าที่ | เขียน/หา finding พร้อมลำดับการกดที่ทำซ้ำได้ | พิสูจน์ด้วยเทส → แก้ → รันครบชุด → merge/commit |
| ตัดสินว่าเป็นบั๊กจริงไหม | ไม่ใช่ | **เทสเป็นคนตัดสิน ไม่ใช่ความเห็นของโมเดลตัวไหน** |

> **แก้กติกาเมื่อ 2026-09-08** — เดิมเขียนว่า "Codex ห้ามแก้ไฟล์" แต่รอบนั้น Codex เขียนลง
> working tree เดียวกับ Claude จริง แล้วเกิดปัญหาที่คาดไว้เป๊ะ: Claude เปิดมาเจอไฟล์ที่ไม่รู้ว่า
> ใครแก้ ต้องเดาจาก timestamp กับสไตล์คอมเมนต์ · โค้ดที่มีบั๊กทำลายข้อมูล 24 record
> **ไม่มีอะไรกั้นเลย** และเกือบปนเข้า commit ถัดไป
> → เลิกห้ามเขียน แต่**บังคับแยก branch** เพราะกฎที่ห้ามสิ่งที่เกิดขึ้นจริงอยู่แล้วไม่ได้ป้องกันอะไร

---

## แบ่ง branch — ทำงานขนานกันโดยไม่ทับกัน

```
Codex  →  git switch -c codex/<ชื่องาน>   →  แก้ + commit บน branch ตัวเอง
Claude →  git diff main..codex/<ชื่องาน>  →  เขียนเทสให้แดง  →  ตัดสิน  →  merge หรือ reject
```

**กติกา**
1. **`main` เป็นของ Claude** — Codex ไม่ commit ลง `main` และไม่ทิ้งงานค้างไว้ใน working tree ของ `main`
2. **1 งาน = 1 branch** ชื่อขึ้นต้น `codex/` เสมอ เพื่อให้ `git branch --list 'codex/*'` เห็นครบ
3. **Codex commit งานตัวเองก่อนส่งรีวิว** — งานที่ยังไม่ commit ไม่มีใครรู้ว่าเขียนเสร็จหรือยัง
   (รอบ 2026-09-08 Claude ต้องดู mtime ของไฟล์เพื่อเดาว่า Codex หยุดเขียนแล้วหรือไม่)
4. อยากแยกโฟลเดอร์จริงไม่ให้ชนกันเลย ใช้ worktree: `git worktree add ../ie-codex codex/<งาน>`
5. **ส่งงานเป็น branch เท่านั้น — ห้ามส่งเป็น ZIP** และ **`git pull` + อ่านไฟล์นี้ก่อนเสนอทุกครั้ง**
   (รอบ 2026-09-23: snapshot ของ Hermes มาเป็น `.zip` ทาง Telegram ลงวันที่ 2026-09-08/09-16 → ข้อเสนอ
   ส่วนใหญ่เป็นของที่ `main` ทำเสร็จไปแล้ว และงานที่ Hermes เขียนเองก็ไม่มีใครเห็นเพราะไม่ได้อยู่บน branch)

---

## เกราะอัตโนมัติ: `pre-commit` hook

ติดตั้งครั้งเดียวต่อ clone (ต้องทำทั้งฝั่ง Codex และ Claude):

```bash
git config core.hooksPath .githooks
```

ตัว hook อยู่ที่ [`.githooks/pre-commit`](.githooks/pre-commit) — commit เข้า repo แล้ว จึงได้กติกาชุดเดียวกันทุกฝ่าย

| ด่าน | ทำอะไร | เมื่อไหร่ |
|---|---|---|
| 1 | บล็อกความลับ/ข้อมูลปฏิบัติการที่ถูก stage (`.env`, `*.jsonl`, `tracking_data.json`, `backups/`) + คีย์ที่โผล่ในเนื้อ diff | ทุกครั้ง |
| 2 | `node --check` ทุกไฟล์ `.js/.cjs/.mjs` ที่ stage | ทุกครั้ง |
| 3 | `node tests/shipment-stage.cjs` (บริสุทธิ์ ไม่ต้องมี server) | ทุกครั้ง |
| 4 | `node tests/pack-invariants.mjs` | เฉพาะตอนแตะ packer (~7 วิ) |
| 5 | เตือนว่าเทส Playwright ต้องรันมือก่อน push | เฉพาะตอนแตะ freight / api-server |

> ⚠ **hook ของ Claude Code ใช้แทนกันไม่ได้** — มันยิงตาม tool call ของ Claude เอง
> จึง**ไม่รู้เลยเวลาตัวอื่นแก้ไฟล์** · git hook ทำงานตอน commit จึงครอบทุกฝ่ายที่เขียนลง repo นี้
> ตั้งใจให้ด่านที่ต้องพึ่ง server/เบราว์เซอร์ **ไม่อยู่ใน hook** เพราะ hook ที่ล้มเพราะเรื่องนอกประเด็น
> จะถูกเลี่ยงด้วย `--no-verify` แล้วเกราะทั้งชุดก็ไร้ค่า

---

## ขั้นตอนที่ใช้จริง (รอบล่าสุดได้ผลดี)

1. **Codex** ตรวจแล้วเขียน finding ตามรูปแบบด้านล่าง → AOF วางกลับมาในแชต Claude
2. **Claude เขียนเทสให้แดงก่อนเสมอ** — ถ้าเขียนแล้วไม่แดง แปลว่าเป็นข้อเสนอลวง ตอบกลับพร้อมหลักฐานว่าทำไมไม่ใช่บั๊ก
3. แก้จนเทสเขียว → รัน `freight-ui` + `freight-pdf` + `freight-e2e` + `freight-review` ครบ → commit
4. บันทึกผลลงตาราง "รอบที่ผ่านมา" ในไฟล์นี้ ทั้งข้อที่จริงและข้อที่ไม่จริง

> **ห้ามแก้ตามข้อเสนอโดยไม่พิสูจน์ก่อน** — ตัวตรวจอัตโนมัติมี false positive เสมอ
> ถ้าแก้ทุกข้อโดยไม่พิสูจน์ จะได้โค้ดที่เปลี่ยนไปเรื่อย ๆ โดยไม่ได้ดีขึ้น

---

## รูปแบบ finding ที่ใช้ได้จริง

ต่อ 1 ข้อ ขอ:
- **ไฟล์:บรรทัด** (บอกทั้งของ commit ที่รีวิวและของ HEAD ปัจจุบัน ถ้าเลื่อน)
- **ลำดับการกดหรือข้อมูลนำเข้า** ที่ทำซ้ำได้จริง
- **ผลที่ผิดคืออะไร** (ไม่ใช่ "อาจจะ" — บอกว่าผลลัพธ์ผิดยังไง)
- **ความรุนแรง** สูง / กลาง / ต่ำ
- **วิธียืนยันด้วยเทสใน `tests/`**

ไม่รับ: ข้อเสนอ refactor เชิงสไตล์ · เรื่องที่พิสูจน์ไม่ได้ว่าทำให้ผลลัพธ์ผิดหรือข้อมูลหาย ·
การเติมข้อเสนอมาให้ครบจำนวน (ถ้าหมวดไหนไม่พบ ให้บอกว่าตรวจแล้วไม่พบ)

---

## รันเทสได้โดยไม่แก้ไฟล์ใด ๆ

ตั้งแต่ 2026-09-04 ภาพหน้าจอเป็น **opt-in** แล้ว — ไม่ตั้ง `SHOT_DIR` = ไม่เขียนไฟล์ลงดิสก์เลย
ตัวรีวิวที่ถูกสั่ง "ห้ามแก้ไฟล์" จึงรันเทสเองได้ (เดิมเขียน `pw-*.png` ลง CWD จึงรันไม่ได้)

```bash
node tests/shipment-stage.cjs                            # กติกาสถานะ — บริสุทธิ์ ไม่ต้องมี server เลย
node tests/scan-policy.cjs                               # นโยบายรอบสแกน (upsert/ledger/ETS) — บริสุทธิ์เช่นกัน
NODE_PATH=./node_modules node tests/freight-ui.cjs       # รูปร่างหน้าจอ + กติกาการเตือน
NODE_PATH=./node_modules node tests/freight-review.cjs   # finding จากรอบรีวิวก่อน ๆ
NODE_PATH=./node_modules node tests/freight-pdf.cjs      # ขั้นบันได + อ่าน PDF (mock ไม่เสียเงิน)
NODE_PATH=./node_modules node tests/freight-e2e.cjs      # ยอดเงินเทียบ Excel
```

⚠ `freight-e2e` และ `freight-pdf` **บันทึกใบทดสอบขึ้น server แล้วลบทิ้งตอนจบ** (แตะ `freight_quotes.json` ชั่วคราว)
ถ้าต้องการอ่านอย่างเดียวจริง ๆ ให้รันแค่ `freight-ui` กับ `freight-review`

---

## รอบที่ผ่านมา

### 2026-09-08 — รีวิวงาน "เลื่อนสถานะอัตโนมัติจาก ETS" (ตัวรีวิว: Hermes · 6 finding)

ตรวจงานที่ยังไม่ commit ใน working tree ของ `main` (`lib/shipment-rules.cjs` + แก้ `api-server.js`,
`scan-shipment-docs.mjs`, `import-export-os.html`) · **4 จริง · 1 ไม่ใช่บั๊ก · 1 จริงแต่จัดชั้นเกิน**
· และ Claude เจอเพิ่ม **2 ตัวที่ร้ายแรงกว่าทุกข้อที่ถูกรายงาน**

**⚠ การอ้างอิงของ Hermes ชี้ไปที่ snapshot ของตัวเอง ไม่ใช่ `main`** *(แก้คำที่ Claude เขียนผิดไว้เอง — ดูรอบ 2026-09-23)*
รอบนั้น Claude สรุปว่า "ชื่อไฟล์/ฟังก์ชัน/เลขบรรทัดสร้างขึ้นเอง" ซึ่ง**ผิด** · ตรวจกับ snapshot จริงแล้ว
`lib/shipment-status.mjs:22–41` และ `hasCompleteBlData()` (บรรทัด 8) **มีอยู่จริง** — แต่อยู่ในสำเนาของ Hermes
ที่ไม่เคยเข้า `main` เลย (`git log --all` = 0 commit) · มีแค่ `actualArrivalSource` ที่ไม่มีอยู่จริงทั้งสองฝั่ง
→ บทสรุปที่ถูกคือ **ต้องถามว่า "อ้างจาก tree ไหน" ก่อนตรวจ** ไม่ใช่สันนิษฐานว่าตัวรีวิวกุการอ้างอิง

| # | finding | ผล | หลักฐาน |
|---|---|---|---|
| 1 | API ตอบ 2xx ไม่ได้แปลว่าเขียนสำเร็จ — scanner ทิ้ง response แล้ว mark ไฟล์เป็น processed | ✅ **จริง และหนักกว่าที่รายงาน** | server ตอบ `{ok,applied,total,rejected}` แต่ scanner log "สำเร็จ" ทันที (`scan:1149`) · เส้น `idMismatch` (`api-server:1877`) `return` เงียบ — ไม่เข้า `rejected` ไม่นับ `applied` ตอบ 200 · `saveSeen()` (`scan:1164`) อยู่**นอก** try/catch → โฟลเดอร์ถูก mark แม้ upsert พังทุกใบ |
| 2 | `actualArrivalDate` ที่กรอกมือ trigger สถานะอัตโนมัติได้ | ⚠️ **สาระจริง แต่จัดชั้นเกิน** | `sanitizeOrigin()` ไม่จำกัดค่า → ปลอม `_origin:'scan:x'` ได้จริง · **แต่** endpoint ผูก `127.0.0.1` ไม่มี auth บน loopback อยู่แล้ว ใครยิงได้ก็ตั้ง `stage` ตรงๆ ได้ตั้งแต่แรก → เป็นปัญหา **provenance** ไม่ใช่ security escalation |
| 3 | Air/courier ไม่ผ่านกติกาความครบ เพราะบังคับ vessel/voyage ทุกใบ | ✅ **จริง และหนักกว่าที่รายงาน** | ไม่ใช่แค่ "เข้า automation ไม่ได้" — `autoStage` คืน `'po'` แล้ว**เขียนทับสถานะจริง** (ดูบั๊ก A) · เจอ `mode=courier` โดน `received → po` 1 ใบ |
| 4 | ETS session ที่เสียถูกใช้ซ้ำทั้งรอบ | ✅ **จริง** | `scan:1054` `if (/90 วินาที/.test(e.message))` ปิด session **เฉพาะ timeout** · selector/navigation/parse ล้มได้ error คนละข้อความ → `etsSession` ค้าง ถูกใช้ต่อทุกโฟลเดอร์ที่เหลือ |
| 5 | `shipment_runs` บันทึกก่อนยืนยัน upsert | ❌ **ไม่ใช่บั๊ก — เจตนาที่บันทึกไว้แล้ว** | คอมเมนต์ `scan:1086` เขียนตรงๆ ว่า *"เก็บทุกใบไม่ว่าจะเขียนลงการ์ดได้หรือไม่"* · ตรงกับ `CLAUDE.md`: การ์ด 1 ใบเก็บได้ชิปเม้นเดียว ข้อมูลชิปเม้นอื่นต้องไม่หาย · เปลี่ยนเป็น persist-after-confirm = **ทำลายจุดประสงค์เดิม** (ข้อเสนอเพิ่มฟิลด์ status เป็นของดี แต่เป็นส่วนเสริม) |
| 6 | เทสยังไม่ครอบคลุม | ✅ **จริง** | `tests/lib/shipment-rules.cjs` มี 7 assert ใน 15 บรรทัด แต่**พิมพ์ว่า `6/6 passed`** (นับผิดเอง) · ไม่มีเทสฝั่ง server เลย ซึ่งเป็นที่ที่บั๊กจริงอยู่ · วางผิดที่ด้วย (`tests/lib/` เป็นที่เก็บ helper เช่น `pallet-optimum.mjs`) |

#### 🔴 บั๊ก A (Claude เจอ) — การ์ด 24 ใบถูก "ถอยสถานะ" · ละเมิดกฎข้อ 3

เขียนเทสให้แดงก่อนตามกติกา → **แดงจริง**:

```
stage ที่ autoStage คืนได้: etd, arrived, customs
STAGES ที่ระบบมี:         po, etd, arrived, customs, received
record ทั้งหมด: 1073 · มี _stageManual: 0
>>> การ์ดที่จะถูกถอยสถานะถ้า scanner แตะ: 24 ใบ
    received → etd = 12 · customs → etd = 6 · received → po = 5 · customs → po = 1
```

สามอย่างประกอบกัน:
1. **`r.stage = autoStage(...)` อยู่ *หลัง* ตัวกรอง `_fillEmptyOnly`** (`api-server:1849–1892`)
   → เกราะ "เติมเฉพาะช่องว่าง" ที่คุ้มทุกฟิลด์อื่น **ไม่คุ้ม `stage` เลย**
2. **`_stageManual` = 0 จาก 1,073 record** — เกราะที่โค้ดใหม่พึ่งพา ไม่มีในข้อมูลเดิมสักใบ
   (frontend เพิ่งเริ่มติดธงให้เฉพาะการแก้ครั้งใหม่)
3. **`autoStage` คืน `'received'` ไม่ได้เลย** — ไม่มีทางรักษาสถานะปลายทางไว้ได้

`received` = สินค้าถึงคลังแล้ว = สถานะสุดท้าย · ขัดกับ `CLAUDE.md` ที่บันทึกว่า
**"สถานะจาก Odoo เป็นพื้น ไม่ใช่เพดาน"** และขัดกฎข้อ 3 ท้ายไฟล์นี้ตรงๆ

> **ยังไม่ลงมือทำลายข้อมูล** — server ที่รันอยู่เริ่ม 10:03:38 ก่อนไฟล์ถูกแก้ (11:14)
> จึงยังใช้โค้ดเก่า · audit วันนั้นแตะ `stage` = 0 ครั้ง · **เป็นระเบิดเวลา** จะทำงานทันทีที่ restart

#### 🔴 บั๊ก B (Claude เจอ) — ทุกโฟลเดอร์ upsert แม้สกัดข้อมูลไม่ได้เลย

`fields.documentReceivedAt` ถูกตั้งทันทีหลังสร้าง `fields` ว่าง → `Object.keys(fields).length`
ไม่มีวันเป็น 0 → สาขา `else { log('ไม่พบข้อมูลใหม่ที่ดึงได้เลย') }` **ตายสนิท**

#### 🔴 บั๊ก C — เทสที่เขียนใหม่จับได้เอง: `isoDate()` รับวันที่ที่ไม่มีอยู่จริง

`/^\d{4}-\d{2}-\d{2}$/` ล้วนรับ `2026-13-45` ผ่าน → `addDays()` ได้ Invalid Date →
`toISOString()` โยน RangeError → **upsert ทั้งคำขอพังเป็น 400** · เป็นบั๊กชนิดเดียวกับที่
`parseFlexibleDate` เคยพลาดและแก้ไปแล้ว (บันทึกใน `CLAUDE.md`) แต่โมดูลใหม่พาเข้ามาอีก

#### ที่แก้ (commit นี้)

- **`nextStage(current, proposed)`** ใน `lib/shipment-rules.cjs` = เกราะเดียวที่แก้บั๊ก A ครบทั้ง 3 สาเหตุ:
  **สถานะอัตโนมัติเลื่อนได้เฉพาะไปข้างหน้า** เท่าเดิมหรือถอยหลัง = คืน `null` แล้วผู้เรียก **ไม่แตะฟิลด์เลย**
  · ไม่ต้องพึ่ง `_stageManual` ในทิศถอยหลังอีก (`_stageManual` ยังกันทิศไปข้างหน้าตามเจตนาเดิม)
- **แปลงชื่อสถานะรุ่นเก่าก่อนเทียบ** (`canonStage`: `booking`/`transit` → `etd`) ไม่งั้นสถานะเก่าจะเป็น
  "ไม่รู้จัก" แล้วหลุดเกราะ · สถานะที่ไม่รู้จักจริง (เช่นบอร์ด export) = ไม่แตะ
- **`CORE_FIELDS_AIR`** — air/courier ไม่บังคับ `vessel`/`voyage` (แก้ finding 3 ที่ต้นเหตุ ไม่ใช่แค่กันอาการ)
- **แก้ `changed` ให้ตรงกับที่เขียนจริง** — `changed` คำนวณก่อนบล็อกนี้ การเลื่อนสถานะจึงเคย
  **ไม่ขึ้น audit log และไม่ได้ stamp `_src`** = เปลี่ยนสถานะโดยไม่มีที่มา
- **`isoDate()` ตรวจว่าเป็นวันที่ที่มีอยู่จริง** (เทียบกลับหลังแปลง → กัน `2026-02-30` ที่ JS เลื่อนเอง)
- **`documentReceivedAt` ติดเฉพาะเมื่อสกัดได้จริง** อย่างน้อย 1 ฟิลด์
- **ย้ายเทสไป `tests/shipment-stage.cjs`** (25 assert) — ยก 7 assert เดิมมาครบ ไม่ทิ้งของใคร

#### รอบต่อ — แก้ finding 1 + 4 (บั๊กเดิม ไม่ได้เกิดจากงานชุดนี้)

ทั้งสองข้อเป็นบั๊กชนิดเดียวกัน: **ระบบเดินต่อเหมือนไม่มีอะไรผิด ทั้งที่ผิดไปแล้ว**
นโยบายถูกแยกออกมาเป็นฟังก์ชันบริสุทธิ์ที่ `lib/scan-policy.cjs` → เทสได้โดยไม่ต้องมี AI/เบราว์เซอร์

**finding 1 — `ok`/`applied`/`rejected` ถูกทิ้ง + `saveSeen` รันแม้ upsert พัง**

- **server รายงาน `skipped` กลับมาแล้ว** — เส้น `idMismatch` เดิม `return` เงียบ (ไม่เข้า `rejected`
  ไม่นับ `applied`) ตอบ 200 → ผู้เรียกแยกไม่ออกว่า "เขียนแล้ว" หรือ "ข้ามไป"
- **`classifyUpsert(res, po)`** แยกผลเป็น 3 ทาง: `ok` / `skip` (ข้ามอย่างถูกต้อง) / `fail`
- **`folderRetryDecision()`** — พังแล้ว **ไม่ตีตรา ledger** จะลองใหม่รอบหน้า แต่มีเพดาน 5 รอบ
  แล้วยอมแพ้ **อย่างมีเสียง** (`[GIVE-UP]`)

> ⭐ **จุดตัดสินที่สำคัญที่สุดของการแก้นี้: "ข้ามอย่างถูกต้อง" ต้องไม่ถูกนับเป็นล้มเหลว**
> โฟลเดอร์ที่เป็นคนละชิปเม้นโดยธรรมชาติเกิดขึ้นเป็นเรื่องปกติ ถ้าเหมารวมเป็น fail จะไม่ตีตรา ledger
> แล้วเรียก AI ซ้ำ **ทุก 20 นาทีตลอดไป** = เสียเงินฟรี · การแก้แบบ "ไม่ mark เมื่อพัง" เฉยๆ
> จึงอันตรายอีกทางหนึ่ง ต้องมีทั้งการแยกประเภทและเพดาน

**finding 4 — ⚠️ Hermes ชี้ผิดจุด และบั๊กจริงหนักกว่าที่รายงาน**

Hermes เขียนว่า *"หาก ETS error ที่ไม่ใช่ timeout 90 วินาที ระบบยังใช้ session เดิมต่อ"* ซึ่งสื่อว่า
ปัญหาอยู่ในบล็อก `catch` · **แต่ `searchVesselActualDate()` ไม่ throw เลย** — จับ exception ทุกตัว
แล้วคืน `{status:'error', error}` (`lib/ets-lookup.mjs:274-279`) · `catch` จึงไม่มีวันเห็นหน้าเว็บที่พัง

ผลจริงหนักกว่า: session ที่ค้างกลางทางถูกใช้ต่อ**ทุกโฟลเดอร์ที่เหลือ** และทุกการ์ดได้
`etsStatus:'error'` **อย่างเงียบสนิท ไม่มี WARN ไม่มี exception** ตลอดรอบ

- **`etsSessionAction(result, err, reopens)`** ตัดสินจาก **ผลที่คืนมา** ไม่ใช่แค่ exception
- `not_found` (เรือยังไม่ถึง / ไม่พบเที่ยวที่ตรง) = ผลปกติ → **ห้ามปิด session**
- `error` = ปิดทิ้ง เปิดใหม่โฟลเดอร์ถัดไป · เพดาน 3 ครั้งแล้วเลิกค้น ETS ทั้งรอบ (เปิด chromium ใหม่แพง)

#### 🔴 บั๊กที่ **เทสจริงบน server** จับได้ (อ่านโค้ดไม่เจอ)

ตั้งชื่อตัวเก็บ record ที่ข้ามว่า `skipped` แล้ว**ชนกับ `const skipped = [], forced = []`**
ในบล็อก `_fillEmptyOnly` (ตัวนั้นคือรายชื่อ *ฟิลด์* ที่ไม่ถูกทับ — คนละความหมาย)
→ TDZ: `Cannot access 'skipped' before initialization` **ทุกครั้งที่เข้าเส้น idMismatch**
เปลี่ยนเป็น `skippedRecords` แล้วเทสข้อ 6 มีเคสกันไว้แล้ว

> **บทเรียน:** unit test ผ่านหมด 25/25 ตอนนั้น เพราะเทสตรวจ *ซอร์ส* ไม่ได้ *รัน* endpoint จริง
> — ต้องยิงของจริงด้วยเสมอ

#### ทดสอบ (finding 1 + 4)
`tests/scan-policy.cjs` **26/26** · ยิง endpoint จริง 3 เคส: คนละชิปเม้น → `skipped` พร้อมเหตุผล ·
ค่าติดลบ → `rejected` · ปกติ → `applied:1` · **ข้อมูลไม่เปลี่ยนแม้ไบต์เดียวทั้ง 3 เคส** ·
`classifyUpsert` อ่าน response จริงได้ถูกทั้งสามทาง · Scheduled Task รันโค้ดใหม่เองเมื่อ 15:44:42
แล้วบล็อกเก็บกวาด ledger ทำงานถูก (`ตัวนับความล้มเหลวที่ตกค้าง 0 โฟลเดอร์`) · `--dry-run` ผ่าน 0 บาท

---

### 2026-09-04 — Codex รอบแรก (4 finding)
ตรวจ `e2b3b50` + `b3d16a8` · **แดง 4/4 ตอนเขียนเทส = จริงทั้งหมด ไม่มี false positive** · แก้ที่ `a82131e`

| # | finding | ผล | เทสที่พิสูจน์ |
|---|---|---|---|
| 1 | `switchType()` ไม่นับแถวที่เพิ่มเอง/หัวเอกสารว่าเป็นข้อมูล → สลับแม่แบบแล้วหายเงียบ | ✅ จริง | `freight-review` ข้อ 1 |
| 2 | ลบแถวที่เพิ่มเองไม่ถาม ไม่มี undo | ✅ จริง | `freight-review` ข้อ 2 |
| 3 | `normalizeQuote()` ตัด `prices` ที่เกินคอลัมน์เงียบ | ✅ จริง | `freight-review` ข้อ 3 |
| 4 | ไฟล์ Excel ที่ส่งออกไม่มีคำเตือนที่ขึ้นบนหน้าจอ | ✅ จริง | `freight-review` ข้อ 4 |

---

## ตรวจแล้ว — ไม่ใช่บั๊ก อย่ารายงานซ้ำ

รายการนี้คือสิ่งที่ **ดูเหมือนบั๊กแต่เป็นการตัดสินใจที่ตั้งใจแล้ว** พร้อมเหตุผล
ถ้าจะแย้ง ต้องมีหลักฐานใหม่ที่ขัดกับเหตุผลเดิม ไม่ใช่ชี้ซ้ำ

| เรื่อง | ทำไมถึงเป็นแบบนั้น |
|---|---|
| แม่แบบมี `CIS` แต่ไม่มี `CIC` | ทดสอบแล้วโมเดลแมป CIC เข้า CIS ได้ถูก · น่าจะเป็นค่าเดียวกัน (Container Imbalance) ถ้าเพิ่มจะได้สองแถวที่หมายถึงของเดียวกัน แล้วเสี่ยงกรอกซ้ำนับเงินสองรอบ |
| คำเตือน "ยังกรอกไม่ครบ" ขึ้นทั้งที่ LCL กับ FCL มีรายการไม่เท่ากันตามธรรมชาติ | เป็น false positive ที่ยอมรับ — เตือนเฉพาะทิศทางที่อันตราย (ตัวที่ **ถูกสุด** กรอกน้อยกว่า) และมีประโยคบอกว่าถ้าช่องว่างคือรายการที่ไม่มีค่าใช้จ่ายจริง ข้ามได้ |
| ขั้นบันไดใช้ได้แค่แถวค่าระวางหลัก | ในใบเสนอราคาจริง 18 ใบ มีแต่ค่าระวางที่คิดเป็นขั้น · ทำทุกแถวจะเพิ่ม UI ที่ไม่มีใครใช้ |
| `/RT` คิดเป็น CBM ล้วน ไม่ใช่ revenue ton | รู้อยู่และบันทึกไว้ใน `CLAUDE.md` แล้ว · ยังไม่แก้เพราะต้องตัดสินใจเชิงธุรกิจก่อนว่าจะคิด max(CBM, ตัน) เมื่อไหร่ |
| แถวมาตรฐานลบไม่ได้ (ปุ่ม × มีเฉพาะแถวที่เพิ่มเอง) | ตั้งใจ — AOF เคยลบ `O/F (Ocean Freight)` กับ `EXW Charge` หายถาวรมาแล้ว |
| ห้ามใช้ `--color-brand` (emerald) เป็นสีตัวอักษรหรือเส้นขอบบนพื้นสว่าง | วัดแล้ว contrast = **2.00** ตกทั้งเกณฑ์ข้อความ (4.5) และกราฟิก (3.0) · ใช้ได้แค่พื้นปุ่ม CTA / จุด accent |
| จุดสีบอกสถานะ shipment 6 ขั้นยังเป็นสี ทั้งที่ธีมบอก "เขียวเป็นสีเดียวในหน้า" | เบี่ยงจาก spec โดยตั้งใจ — มันเข้ารหัสสถานะงานที่ผู้ใช้อ่านด้วยสายตา ไม่ใช่การตกแต่ง |
| std packer นับชั้นจาก `Object.keys(zSeen).length` | รู้ว่าอาจ under-fill · **จงใจไม่แก้** เพราะแก้ heuristic แกนกลางเสี่ยงกลายเป็น over-report ซึ่งอันตรายกว่า |
| ค่าระวางหลักที่อ่านจาก PDF ไม่ถูกกรอกให้อัตโนมัติเสมอ | วัดแล้วอ่านได้แค่ 59% ของใบ · ตั้งใจให้คนกรอกเอง และมีแถบเตือนย้ำในหน้าตรวจผล |
| `shipment_runs.jsonl` เขียน**ก่อน**ยืนยันว่า upsert สำเร็จ | ตั้งใจ — การ์ด 1 ใบเก็บได้ชิปเม้นเดียว แต่โฟลเดอร์เดียวมีได้หลายชิปเม้น · ใบที่ upsert ปฏิเสธ **ต้องยังหาเจอ** เพื่อเอาไปสร้างการ์ดใหม่ ถ้าเขียนหลังยืนยันจะหายทันที (คอมเมนต์ `scan-shipment-docs.mjs:1086`) |
| `autoStage` ไม่มีทางคืน `'received'` | ตั้งใจ — "รับเข้าคลัง" ยืนยันด้วยเอกสารขาเข้าไม่ได้ ต้องมีคนกดเอง · เกราะที่ทำให้ปลอดภัยคือ `nextStage()` ที่ห้ามถอยหลัง ไม่ใช่การให้ระบบเดาสถานะปลายทาง |
| **Document Review Workspace** (ข้อ 4 ของข้อเสนอ Control Tower) | AOF สั่ง**ถอดโมดูล "ตรวจเอกสาร Shipment" ออกทั้งชุด**เมื่อ 2026-09-04 (ตัดโค้ด ~21,400 ตัวอักษร + ลบ lib 3 ไฟล์) · การเสนอกลับมาต้องมาจาก AOF ไม่ใช่จากตัวรีวิว |
| `owner` / มอบหมายงาน / SLA / reminder / escalation | เลื่อน**โดยตั้งใจ**จนกว่าจะมีผู้ใช้จริงมากกว่าหนึ่งคน — รูปร่างที่ถูกต้องต้องเรียนรู้จากคนใช้ ไม่ใช่เดาล่วงหน้าแล้วรื้อทีหลัง (`CLAUDE.md` 2026-09-22) |

---

## กติกาที่ห้ามถอด (ถ้า finding ไหนขัดกับข้อเหล่านี้ ให้ยกมาคุยก่อน)

1. **ห้ามตัดข้อมูลโดยไม่บอก** — LIMIT / slice / cap ทุกจุดต้องแจ้งผู้ใช้เมื่อตัดของจริง
2. **รายงานเกินจริงอันตรายกว่ารายงานต่ำ** — เมื่อไม่แน่ใจ ให้เลือกทางที่รายงานต่ำกว่า
3. **ห้ามทับค่าที่ผู้ใช้กรอกเอง** — เติมได้เฉพาะช่องว่าง
4. **ห้าม re-render DOM ระหว่างผู้ใช้พิมพ์** — อัปเดตเฉพาะเซลล์ยอดเงินแบบ in-place
5. **คำเตือนต้องตามไปทุกทางที่ตัวเลขออกจากระบบ** — หน้าจอ ไฟล์ Excel และการพิมพ์

---

## Product / UX Review สำหรับ Claude — ยกระดับ Import-Export OS ให้เป็น Shipment Control Tower

### วัตถุประสงค์

โปรเจกต์นี้ไม่ควรเป็นเพียงระบบเก็บข้อมูล shipment แต่ควรเป็น **Shipment Control Tower** ที่ช่วยให้ผู้ใช้รู้ทันทีว่า:

1. shipment ไหนมีปัญหา
2. วันนี้ต้องทำอะไร
3. ต้องติดต่อใคร
4. ข้อมูลใดเชื่อถือได้และมาจากแหล่งใด
5. ขั้นตอนถัดไปคืออะไร

> เป้าหมายไม่ใช่ทำ automation ให้มากที่สุด แต่คือทำให้ผู้ใช้ตัดสินใจได้เร็วขึ้น โดยระบบไม่เดาข้อมูลที่ยังยืนยันไม่ได้

### หลักการผลิตภัณฑ์ที่ต้องรักษา

- **Trust over automation:** ไม่มีหลักฐานยืนยัน = ไม่เลื่อนสถานะและไม่แจ้งเตือนเกินจริง
- **Exception-first:** แสดงปัญหาและงานค้างก่อนข้อมูลที่ไม่มีปัญหา
- **Source-aware:** ทุกค่าหลักต้องรู้ว่าได้มาจากเอกสาร, ETS, Odoo หรือผู้ใช้กรอกเอง
- **Human control:** ระบบ automate ถึง `กำลังเดินพิธีการ`; หลังจากนั้นผู้ใช้ยืนยันเองจนถึง `สินค้าถึงคลัง`
- **No silent data loss:** ห้ามทับข้อมูลผู้ใช้ ห้ามตัดข้อมูลโดยไม่แจ้ง และทุกการแก้ต้องตรวจสอบย้อนหลังได้
- **Action over decoration:** ทุกการ์ดสถานะควรมีปุ่มงานถัดไป ไม่ใช่มีแค่สีหรือ label

### 1. Dashboard แบบ “ต้องทำอะไรวันนี้”

หน้าแรกควรสรุปเป็นกลุ่มงานที่ลงมือทำได้ทันที:

- ต้องดำเนินการวันนี้
- เรือใกล้ถึงภายใน 3 วัน
- รอเอกสารจาก supplier
- รอ freight forwarder ตอบกลับ
- รอตรวจ draft BL / Import Entry / Form D, E, AK
- พบข้อมูลเอกสารไม่ตรงกัน
- ETS ตรวจไม่สำเร็จหรือ match voyage ไม่ได้
- shipment ที่ค้างสถานะนานผิดปกติ

ผู้ใช้ควรเห็นคำตอบภายในประมาณ 5 วินาทีว่า shipment ใดต้องติดตามและต้องทำอะไรต่อ

### 2. สถานะต้องสื่อถึงงานถัดไป

การ์ดแต่ละสถานะควรแสดงเงื่อนไขและ action ที่เกี่ยวข้อง:

| สถานะ | ข้อมูล/งานที่ควรแสดง |
|---|---|
| รอเอกสาร | เอกสารที่ขาด, ผู้รับผิดชอบ, ค้างกี่วัน |
| ETD | ETD, ข้อมูลหลักครบหรือไม่, ตรวจ ETS ได้หรือยัง |
| ถึงท่าเรือ | ETS Actual Arrival, เวลาตรวจล่าสุด, voyage ที่ match |
| กำลังเดินพิธีการ | วันที่เริ่ม, เอกสารที่ขาด, ผู้รับผิดชอบ |
| สินค้าถึงคลัง | วันที่รับจริง, หลักฐานการรับ, ความล่าช้าเทียบแผน |

ปุ่ม action ที่ควรมี: `ตรวจ ETS`, `ตรวจเอกสาร`, `ขอเอกสาร`, `ส่ง Reminder`, `บันทึกการรับเข้า`, `ดูประวัติ`

### 3. แยก Schedule กับ Actual ให้เห็นชัด

ข้อมูลวันที่ต้องไม่ใช้ชื่อ ETA รวมกันจนทำให้ผู้ใช้เข้าใจผิด:

- `ETD` = กำหนดการเรือออก
- `eta` = Schedule ETA
- `etsActualArrivalDate` = Actual Arrival จาก ETS
- `actualDate` = วันที่รับเข้าคลังจริงโดยผู้ใช้

เมื่อแสดง Actual Arrival ให้แสดง provenance ด้วย เช่น:

```text
ETA: 18 Sep 2026
Source: ETS Actual Arrival
Checked: 16 Sep 2026 09:30
Voyage match: ตรงกัน
```

กรณี match ไม่มั่นใจ ต้องขึ้นคำเตือนและห้ามปรับสถานะอัตโนมัติ เช่น:

```text
พบเรือชื่อใกล้เคียง แต่ Voyage ไม่ตรง — ยังไม่ยืนยัน Actual Arrival
```

### 4. Document Review Workspace

ควรมีหน้าตรวจร่างเอกสารก่อนยืนยันหรือส่งผ่าน Email, WeChat และ LINE โดยเทียบข้อมูลข้ามเอกสารในที่เดียว:

- ชื่อ Buyer / Seller
- BL Number
- Invoice Number / Date
- จำนวนสินค้า
- น้ำหนัก
- มูลค่าและสกุลเงิน
- ประเทศต้นทาง
- Incoterms
- HS Code
- Port of Loading / Discharge
- Vessel / Voyage

ผลตรวจควรแบ่งเป็น:

- `ตรงกัน`
- `พบความแตกต่าง`
- `ข้อมูลหาย`
- `ต้องตรวจด้วยคน`

ตัวอย่างกฎที่ต้องหยุดการยืนยัน:

```text
Invoice ระบุ 1,200 KG แต่ Packing List ระบุ 1,250 KG
ไม่ควรยืนยันเอกสารจนกว่าจะตรวจสอบความแตกต่าง
```

### 5. Message Templates และการติดตามการตอบกลับ

เพิ่ม template ที่เติมข้อมูล shipment อัตโนมัติสำหรับข้อความที่ใช้ซ้ำบ่อย:

- Confirm draft BL
- Confirm draft Import / Export Entry
- Confirm draft Form D / E / AK
- ขอเอกสารที่ยังขาด
- แจ้งเรือเลื่อน
- แจ้งเรือใกล้ถึง
- ติดตาม supplier / freight forwarder ที่ยังไม่ตอบ

แต่ละข้อความควรมี `Copy`, `เปิด Email`, `แนบเอกสาร`, และ `บันทึกว่าใครส่งเมื่อไร` โดยเริ่มจาก copy-to-clipboard และ audit log ก่อนเชื่อม API ของแต่ละ platform

### 6. Exception-first และ SLA

ระบบควรคำนวณงานค้างและความเสี่ยง เช่น:

- รอเอกสารเกิน SLA
- supplier ยังไม่ตอบเกินกำหนด
- ETS Actual Arrival ผ่านแล้วแต่ยังไม่ตรวจ
- สถานะเดิมนานกว่าค่าปกติ
- Vessel/Voyage ไม่ตรง
- เอกสารขัดแย้งกัน
- Actual Arrival ช้ากว่า Schedule ETA

ทุก exception ต้องมีเหตุผล, owner, due date และปุ่มแก้ไข ไม่ควรเป็นเพียง badge สีแดง

### 7. การกรอกข้อมูลต้องเร็วและไม่ซ้ำ

ลำดับการใช้งานที่ควรได้:

1. ลากไฟล์ BL เข้าโฟลเดอร์หรือหน้าเว็บ
2. ระบบตรวจพบ shipment เดิมจาก BL/PO/Invoice
3. สกัดข้อมูลหลักและแสดงผลพร้อม source
4. ตรวจครบ 6/6 fields
5. ให้ผู้ใช้กดยืนยันก่อนเปลี่ยนเป็น `ETD`
6. ตรวจ ETS ตาม schedule

ควรรองรับ drag & drop, scan folder, duplicate detection, Excel import, bulk edit, และการจำรูปแบบเอกสารของ supplier เดิม

### 8. Manual override ต้องโปร่งใส

ทุก shipment ที่ถูกแก้สถานะเองควรแสดง:

- สถานะปัจจุบัน
- ผู้แก้ไข
- เวลาแก้ไข
- เหตุผล
- `automatic` หรือ `manual override`

ต้องมีปุ่ม `กลับไปใช้สถานะอัตโนมัติ` เพื่อยกเลิก override โดยไม่ลบประวัติเดิม

### 9. KPI ที่สร้างจาก audit log

รายงานควรสร้างจากข้อมูลจริงโดยไม่ต้องกรอกซ้ำ:

- จำนวน shipment
- เวลาเฉลี่ย ETD → Actual Arrival
- เวลา Actual Arrival → เริ่มพิธีการ
- เวลาเริ่มพิธีการ → สินค้าถึงคลัง
- Supplier ส่งเอกสารช้าเฉลี่ย
- Forwarder ตอบกลับช้าเฉลี่ย
- จำนวน document mismatch
- จำนวนครั้งแก้ BL
- จำนวน shipment ล่าช้าและสาเหตุ
- เปรียบเทียบ KOB/BTV, supplier และ forwarder

ต้องดูได้รายเดือน, ต้นปีถึงปัจจุบัน, กลางปี และ export เป็นรายงานเก็บภายในสำหรับวัด KPI

### 10. Roadmap ที่แนะนำ

#### Phase 1 — Trust & Accuracy

- data model กลาง
- field provenance
- ตรวจข้อมูลหลักครบ 6/6
- ETS matching แบบปลอดภัย
- auto-stage ถึง `กำลังเดินพิธีการ`
- manual override
- audit log
- duplicate detection

#### Phase 2 — Daily Productivity

- Dashboard “ต้องทำวันนี้”
- reminder และ SLA
- message templates
- document review
- bulk actions
- drag & drop scan
- internal notification

#### Phase 3 — Automation

- scheduled ETS polling
- Email integration
- LINE integration
- WeChat ตามความพร้อมของ API
- auto-create shipment จากเอกสาร
- escalation เมื่อเกิน SLA

#### Phase 4 — Management Intelligence

- KPI dashboard
- supplier scorecard
- forwarder performance
- delay analysis/prediction
- lead-time และ cost analysis
- monthly / half-year report

### Acceptance Criteria สำหรับการพัฒนาต่อ

Claude ควรถือว่างานหนึ่งเสร็จต่อเมื่อ:

- มีเทสที่ทำให้บั๊กหรือ requirement นั้น fail ก่อนแก้
- ข้อมูลจากเอกสาร, ETS และผู้ใช้แยก source ได้
- ระบบไม่ปรับสถานะเมื่อข้อมูลหลักไม่ครบหรือ voyage ไม่ match
- ระบบไม่ทับค่าที่ผู้ใช้กรอกเอง
- สถานะไม่ถอยหลังโดยอัตโนมัติ
- การเปลี่ยนสถานะมี audit trail
- ข้อความเตือนบอกสาเหตุและ action ถัดไป
- ทดสอบทั้ง unit, API integration และหน้าเว็บจริง
- ไม่มีข้อมูลสำคัญหายเมื่อ upsert, scan หรือ refresh

### ลำดับความสำคัญที่ควรเริ่มก่อน

1. ตรวจสอบและทำให้ provenance ของข้อมูลหลักสมบูรณ์
2. สร้าง Document Review Workspace
3. สร้าง Dashboard งานค้างและ exception
4. เพิ่ม template การสื่อสารพร้อม audit log
5. เพิ่ม SLA และ reminder
6. สร้าง KPI จากข้อมูลที่เก็บได้จริง

**คำแนะนำสำหรับ Claude:** อย่าเริ่มจากการเพิ่มสี, animation หรือฟีเจอร์จำนวนมาก ให้เริ่มจากเส้นทางที่ถ้าผิดแล้วกระทบธุรกิจจริง: `เอกสาร → ข้อมูลหลัก → ETS → สถานะ → การสื่อสาร → audit/KPI` และทุกข้อเสนอใหม่ต้องพิสูจน์ด้วยเทสหรือข้อมูลจากการใช้งานจริงก่อน merge

---

## 2026-09-23 — ผลประเมินข้อเสนอ "Shipment Control Tower" (ตัวเสนอ: Hermes)

ข้อเสนอเต็มอยู่หัวข้อก่อนหน้า · ส่วนนี้คือคำตอบว่า **อะไรทำไปแล้ว · อะไรเลื่อนโดยตั้งใจ ·
อะไรหยิบมาใช้ได้จริง · อะไรห้าม merge** — เขียนไว้เพื่อให้รอบหน้าไม่เสนอของเดิมซ้ำ

> **ข้อเสนอเขียนจาก snapshot ลงวันที่ 2026-09-08 / 09-16** ที่ส่งมาเป็นไฟล์ `.zip` ทาง Telegram
> (`save-main/`) ไม่ใช่ branch → ไม่เห็นงานที่ `main` ทำหลังจากนั้น
> **ข้อเสนอส่วนใหญ่จึงเป็นของที่ทำเสร็จไปก่อนแล้ว** ไม่ใช่เพราะเสนอผิด แต่เพราะช่องทางส่งงานไม่ sync
> → แก้ด้วยกติกาข้อ 5 หัวข้อ "แบ่ง branch"
> *(ตรวจแล้ว: zip **ไม่มี** `.env` / `tracking_data.json` / `*.jsonl` / `backups/` — มีแต่ `.env.example`
> กับ `rds-ca.pem` ซึ่งเป็น CA สาธารณะของ Amazon RDS ไม่ใช่ความลับ · ไม่มีอะไรรั่ว)*

### Phase 1 (Trust & Accuracy) — ครบทั้ง 8 ข้อก่อนข้อเสนอถูกเขียน

| ข้อเสนอ | สถานะ | อยู่ที่ไหน |
|---|---|---|
| data model กลาง | ✅ | `tracking_data.json` + atomic temp+rename |
| field provenance | ✅ 2026-07-31 | `_src` รูปแบบ `<origin>[:<ref>]@<iso>` |
| ตรวจข้อมูลหลักครบ 6/6 | ✅ **และแก้ให้ถูกกว่าที่เสนอ** | `CORE_FIELDS` + **`CORE_FIELDS_AIR`** — ทางอากาศไม่มี vessel/voyage การบังคับ 6/6 ทุกโหมดคือบั๊ก (finding 3 รอบ 2026-09-08) |
| ETS matching แบบปลอดภัย | ✅ | `lib/ets-lookup.mjs:218–236` — ตรงเป๊ะ → prefix (ต้องยาว ≥4) → ไม่ตรงเที่ยวไหน = `not_found` **ห้ามหยิบแถวแรก** |
| auto-stage ถึง `กำลังเดินพิธีการ` | ✅ | `autoStage()` คืนได้สูงสุด `customs` · `received` ต้องคนกดเอง |
| manual override | ✅ **ด้วยเกราะที่แข็งกว่าที่เสนอ** | ข้อเสนอพึ่งธง manual · แต่ `_stageManual` มีจริง **0 จาก 1,073 record** = เกราะที่ไม่มีอยู่ → ใช้ `nextStage()` ratchet ห้ามถอยหลังทุกกรณีแทน ไม่ต้องพึ่งธงเลย |
| audit log | ✅ | `tracking_audit.jsonl` + ฟิลด์ `user` (2026-09-22) |
| duplicate detection | ✅ | `SKIP-OTHER` → `MERGE` (เรือ+voyage / เลข AWB) → `DEDUP` ใน `scan-shipment-docs.mjs` |

### Phase 2 (Daily Productivity) — ทำแล้ว 1 · ถอดตามคำสั่ง 1 · เลื่อนอย่างมีเหตุผล 4

| ข้อเสนอ | สถานะ |
|---|---|
| Dashboard "ต้องทำวันนี้" | ✅ 2026-09-22 — `lib/worklist.js` + แท็บ "งานค้าง" · 1,080 การ์ด → **ต้องตามตอนนี้ 59 · ค้างสะสม 186** |
| drag & drop scan | ✅ บางส่วน — มีในโมดูลค่าเฟรท (ลาก PDF ใบเสนอราคา) · ฝั่ง shipment ใช้โฟลเดอร์อัตโนมัติแทน |
| document review | ❌ **ถอดออกตามคำสั่ง AOF** 2026-09-04 — ดูตาราง "ตรวจแล้ว ไม่ใช่บั๊ก" |
| reminder / SLA · message templates · bulk actions · internal notification | ⏸ เลื่อนจนกว่าจะมีผู้ใช้จริง |

### 🔴 ช่องว่างของตัวข้อเสนอเอง: data model ไม่มี "ใคร"

Phase 1 ไล่ field ครบทุกอย่าง **ยกเว้นคนทำ** แต่ Phase 3 เสนอ escalation และ Phase 4 เสนอ KPI
อย่าง *"Forwarder ตอบกลับช้าเฉลี่ย"* / *"จำนวนครั้งแก้ BL"* — ซึ่ง**ตอบไม่ได้เลยถ้าไม่รู้ว่าใครแก้**
และเป็นข้อมูลที่ **เติมย้อนหลังไม่ได้** (ประวัติที่ผ่านไปแล้วไม่มีชื่อคนอยู่ในนั้น)
ถ้าทำตามลำดับที่เสนอ จะไปรู้ตัวตอน Phase 4 แล้วสายเกินไปหลายเดือน

→ ทำแซงไปแล้วเมื่อ 2026-09-22: `actorOf()` (Basic Auth username — เดิมโค้ด**ทิ้ง username ใช้แต่รหัสผ่าน**)
· `_by` ใน record · `user` ใน audit · `manual:<ผู้ใช้>` ใน `_src` ซึ่งเข้ารูปแบบเดิมพอดี **ไม่ต้อง migrate**

### ⭐ สิ่งที่หยิบจาก snapshot ของ Hermes ได้จริง — 1 อย่าง (ยังไม่แก้ ต้องมีเทสแดงก่อน)

**`selectUniqueVoyageRow()` ปฏิเสธเมื่อ voyage ตรงเป๊ะ *มากกว่า 1 แถว*** (`lib/ets-result.mjs` ใน snapshot)

`main` ใช้ `rowsWithCells.find(...)` → หยิบ **แถวแรก** เงียบ ๆ เมื่อ ETS คืนหลายแถวที่ voyage เดียวกัน
ซึ่งเกิดได้จริงเมื่อ **เรือลำเดียวเที่ยวเดียวแวะหลายท่า** — วัน Actual ของแต่ละท่าไม่เท่ากัน
→ อาจได้วันเรือถึงของท่าที่ไม่ใช่ปลายทางของเรา **โดยไม่มีอะไรเตือน** (กติกาข้อ 2: ห้ามเดาเมื่อกำกวม)

**ยังไม่แก้เพราะยังพิสูจน์ไม่ได้** — ตัวเลือกแถวอยู่ใน `page.evaluate()` จึงเขียนเทสหน่วยไม่ได้
ต้องยก selector ออกมาเป็นฟังก์ชันบริสุทธิ์ก่อน (แบบที่ Hermes ทำ) แล้วเทสด้วยตาราง mock
**เทสที่ต้องแดงก่อนแก้:** 2 แถว voyage เดียวกัน วัน Actual ต่างกัน → ต้องได้ `not_found` ไม่ใช่แถวแรก

> ⚠ **เอาเฉพาะกฎ ห้ามยกทั้งโมดูลมาแทน** — `normalizeVoyage()` ของ snapshot ไม่มีเงื่อนไข `want.length >= 4`
> ที่ `main` มี → `"76S"` จะ prefix-match กับ `"76SN"` ได้ ทั้งที่ S/N คือทิศ South/North = **คนละเที่ยวจริง**
> ของ snapshot **หลวมกว่า** ตรงนี้ · และเงื่อนไข `candidates.length !== rows.length → null` จะปฏิเสธทั้งตาราง
> เพียงเพราะมีแถวเดียวที่ช่อง voyage ว่าง ซึ่งเข้มเกินไปคนละแบบ

### 🔴 สิ่งที่ห้าม merge ตรง ๆ — จะถอยหลังทันที 3 จุด

| ที่ snapshot | ถ้า merge จะเกิดอะไร |
|---|---|
| `shipment-status.mjs:8` — `hasCompleteBlData()` บังคับ `vessel` + `voyage` **ทุกโหมด** | คืนบั๊ก finding 3 ทันที: ชิปเม้นทางอากาศ/courier ที่เอกสารครบจะได้ `'po'` เสมอ แล้ว**เขียนทับสถานะจริง** (เคยทำการ์ด `received` ถอยเป็น `po` มาแล้ว) · `main` แก้ด้วย `CORE_FIELDS_AIR` |
| `shipment-status.mjs:26` — กันถอยหลังเฉพาะ `currentRank >= received` | `customs` ยังถูกดึงถอยได้ (วัดจริงรอบ 2026-09-08: `customs → etd` 6 ใบ, `customs → po` 1 ใบ) · `nextStage()` ของ `main` กันทุกขั้น ไม่ใช่แค่ขั้นสุดท้าย |
| `upsert-result.mjs` — `assertUpsertSucceeded()` throw เมื่อ `applied < 1` | **เปลี่ยน "ข้ามอย่างถูกต้อง" ให้กลายเป็น "ล้มเหลว"** — โฟลเดอร์ที่เป็นคนละชิปเม้นตอบ `applied:0` ซึ่งเป็นเรื่องปกติมาก → ไม่ตีตรา ledger → **เรียก AI ซ้ำทุก 20 นาทีตลอดไป = เสียเงินฟรี** · `classifyUpsert()` ของ `main` แยก `ok` / `skip` / `fail` ออกจากกันด้วยเหตุผลนี้ตรง ๆ |

> ทั้งสามไฟล์เป็นงานที่ Hermes เขียนเองบน snapshot ลงวันที่ **2026-09-16** คือ *หลัง* `main` แก้เรื่องเดียวกัน
> ไปแล้วเมื่อ 2026-09-08 — เป็นหลักฐานตรง ๆ ว่าการส่งงานเป็น zip ทำให้ **ทำงานซ้ำกันโดยไม่มีใครรู้**

### สรุปสำหรับรอบหน้า

หลักการทั้ง 6 ข้อของข้อเสนอ (*trust over automation · exception-first · source-aware · human control ·
no silent data loss · action over decoration*) **ตรงกับสิ่งที่โปรเจกต์นี้ทำอยู่แล้วทุกข้อ** และตรงกับ
"กติกาที่ห้ามถอด" ด้านบน — ส่วนที่ขาดไม่ใช่หลักการ แต่คือ **การเห็นสถานะปัจจุบันก่อนเสนอ**

ก่อนเสนอรอบหน้า: `git pull` → อ่านหัวข้อ **"ตรวจแล้ว — ไม่ใช่บั๊ก"** กับ **"กติกาที่ห้ามถอด"** →
เสนอเฉพาะสิ่งที่อ้างอิงโค้ดบน `main` ได้จริง พร้อมบอกว่า **อ้างจาก commit ไหน**
