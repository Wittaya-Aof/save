# ช่องทางรีวิวร่วม — Codex ↔ Claude

ไฟล์นี้คือ **ช่องทางเดียวที่ตัวรีวิวกับตัวแก้คุยกันได้** — ทั้งสองตัวไม่มีทางส่งข้อความถึงกันโดยตรง
แต่เปิดโฟลเดอร์เดียวกัน (`C:\Users\User\Projects\import-export-os`) จึงใช้ไฟล์ใน repo เป็นความจำร่วม

> ตรงกับหลักที่บันทึกไว้ใน `CLAUDE.md` หัวข้อ loop/graph engineering: **"the agent forgets, the graph does not"**
> — บทสนทนาหายไปทุกรอบ แต่ artifact ใน repo อยู่ต่อ

---

## บทบาท

| | Codex | Claude |
|---|---|---|
| แก้ไฟล์ | ได้ — **แต่บน branch ของตัวเองเท่านั้น** | บน `main` (และรีวิว branch ของ Codex) |
| หน้าที่ | เขียน/หา finding พร้อมลำดับการกดที่ทำซ้ำได้ | พิสูจน์ด้วยเทส → แก้ → รันครบชุด → merge/commit |
| ตัดสินว่าเป็นบั๊กจริงไหม | ไม่ใช่ | **เทสเป็นคนตัดสิน ไม่ใช่ความเห็นของโมเดลตัวไหน** |

> **แก้กติกาเมื่อ 2026-09-08** — เดิมเขียนว่า "Codex ห้ามแก้ไฟล์" แต่รอบนั้น Codex เขียนลง
> working tree เดียวกับ Claude จริง แล้วเกิดปัญหาที่คาดไว้เป๊ะ: Claude เปิดมาเจอไฟล์ที่ไม่รู้ว่า
> ใครแก้ ต้องเดาจาก timestamp กับสไตล์คอมเมนต์ · โค้ดที่มีบั๊กทำลายข้อมูล 24 record
> **ไม่มีอะไรกั้นเลย** และเกือบปนเข้า commit ถัดไป
> → เลิกห้ามเขียน แต่**บังคับแยก branch** เพราะกฎที่ห้ามสิ่งที่เกิดขึ้นจริงอยู่แล้วไม่ได้ป้องกันอะไร

---

## แบ่ง branch — ทำงานขนานกันโดยไม่ทับกัน

```
Codex  →  git switch -c codex/<ชื่องาน>   →  แก้ + commit บน branch ตัวเอง
Claude →  git diff main..codex/<ชื่องาน>  →  เขียนเทสให้แดง  →  ตัดสิน  →  merge หรือ reject
```

**กติกา**
1. **`main` เป็นของ Claude** — Codex ไม่ commit ลง `main` และไม่ทิ้งงานค้างไว้ใน working tree ของ `main`
2. **1 งาน = 1 branch** ชื่อขึ้นต้น `codex/` เสมอ เพื่อให้ `git branch --list 'codex/*'` เห็นครบ
3. **Codex commit งานตัวเองก่อนส่งรีวิว** — งานที่ยังไม่ commit ไม่มีใครรู้ว่าเขียนเสร็จหรือยัง
   (รอบ 2026-09-08 Claude ต้องดู mtime ของไฟล์เพื่อเดาว่า Codex หยุดเขียนแล้วหรือไม่)
4. อยากแยกโฟลเดอร์จริงไม่ให้ชนกันเลย ใช้ worktree: `git worktree add ../ie-codex codex/<งาน>`

---

## เกราะอัตโนมัติ: `pre-commit` hook

ติดตั้งครั้งเดียวต่อ clone (ต้องทำทั้งฝั่ง Codex และ Claude):

```bash
git config core.hooksPath .githooks
```

ตัว hook อยู่ที่ [`.githooks/pre-commit`](.githooks/pre-commit) — commit เข้า repo แล้ว จึงได้กติกาชุดเดียวกันทุกฝ่าย

| ด่าน | ทำอะไร | เมื่อไหร่ |
|---|---|---|
| 1 | บล็อกความลับ/ข้อมูลปฏิบัติการที่ถูก stage (`.env`, `*.jsonl`, `tracking_data.json`, `backups/`) + คีย์ที่โผล่ในเนื้อ diff | ทุกครั้ง |
| 2 | `node --check` ทุกไฟล์ `.js/.cjs/.mjs` ที่ stage | ทุกครั้ง |
| 3 | `node tests/shipment-stage.cjs` (บริสุทธิ์ ไม่ต้องมี server) | ทุกครั้ง |
| 4 | `node tests/pack-invariants.mjs` | เฉพาะตอนแตะ packer (~7 วิ) |
| 5 | เตือนว่าเทส Playwright ต้องรันมือก่อน push | เฉพาะตอนแตะ freight / api-server |

> ⚠ **hook ของ Claude Code ใช้แทนกันไม่ได้** — มันยิงตาม tool call ของ Claude เอง
> จึง**ไม่รู้เลยเวลาตัวอื่นแก้ไฟล์** · git hook ทำงานตอน commit จึงครอบทุกฝ่ายที่เขียนลง repo นี้
> ตั้งใจให้ด่านที่ต้องพึ่ง server/เบราว์เซอร์ **ไม่อยู่ใน hook** เพราะ hook ที่ล้มเพราะเรื่องนอกประเด็น
> จะถูกเลี่ยงด้วย `--no-verify` แล้วเกราะทั้งชุดก็ไร้ค่า

---

## ขั้นตอนที่ใช้จริง (รอบล่าสุดได้ผลดี)

1. **Codex** ตรวจแล้วเขียน finding ตามรูปแบบด้านล่าง → AOF วางกลับมาในแชต Claude
2. **Claude เขียนเทสให้แดงก่อนเสมอ** — ถ้าเขียนแล้วไม่แดง แปลว่าเป็นข้อเสนอลวง ตอบกลับพร้อมหลักฐานว่าทำไมไม่ใช่บั๊ก
3. แก้จนเทสเขียว → รัน `freight-ui` + `freight-pdf` + `freight-e2e` + `freight-review` ครบ → commit
4. บันทึกผลลงตาราง "รอบที่ผ่านมา" ในไฟล์นี้ ทั้งข้อที่จริงและข้อที่ไม่จริง

> **ห้ามแก้ตามข้อเสนอโดยไม่พิสูจน์ก่อน** — ตัวตรวจอัตโนมัติมี false positive เสมอ
> ถ้าแก้ทุกข้อโดยไม่พิสูจน์ จะได้โค้ดที่เปลี่ยนไปเรื่อย ๆ โดยไม่ได้ดีขึ้น

---

## รูปแบบ finding ที่ใช้ได้จริง

ต่อ 1 ข้อ ขอ:
- **ไฟล์:บรรทัด** (บอกทั้งของ commit ที่รีวิวและของ HEAD ปัจจุบัน ถ้าเลื่อน)
- **ลำดับการกดหรือข้อมูลนำเข้า** ที่ทำซ้ำได้จริง
- **ผลที่ผิดคืออะไร** (ไม่ใช่ "อาจจะ" — บอกว่าผลลัพธ์ผิดยังไง)
- **ความรุนแรง** สูง / กลาง / ต่ำ
- **วิธียืนยันด้วยเทสใน `tests/`**

ไม่รับ: ข้อเสนอ refactor เชิงสไตล์ · เรื่องที่พิสูจน์ไม่ได้ว่าทำให้ผลลัพธ์ผิดหรือข้อมูลหาย ·
การเติมข้อเสนอมาให้ครบจำนวน (ถ้าหมวดไหนไม่พบ ให้บอกว่าตรวจแล้วไม่พบ)

---

## รันเทสได้โดยไม่แก้ไฟล์ใด ๆ

ตั้งแต่ 2026-09-04 ภาพหน้าจอเป็น **opt-in** แล้ว — ไม่ตั้ง `SHOT_DIR` = ไม่เขียนไฟล์ลงดิสก์เลย
ตัวรีวิวที่ถูกสั่ง "ห้ามแก้ไฟล์" จึงรันเทสเองได้ (เดิมเขียน `pw-*.png` ลง CWD จึงรันไม่ได้)

```bash
node tests/shipment-stage.cjs                            # กติกาสถานะ — บริสุทธิ์ ไม่ต้องมี server เลย
NODE_PATH=./node_modules node tests/freight-ui.cjs       # รูปร่างหน้าจอ + กติกาการเตือน
NODE_PATH=./node_modules node tests/freight-review.cjs   # finding จากรอบรีวิวก่อน ๆ
NODE_PATH=./node_modules node tests/freight-pdf.cjs      # ขั้นบันได + อ่าน PDF (mock ไม่เสียเงิน)
NODE_PATH=./node_modules node tests/freight-e2e.cjs      # ยอดเงินเทียบ Excel
```

⚠ `freight-e2e` และ `freight-pdf` **บันทึกใบทดสอบขึ้น server แล้วลบทิ้งตอนจบ** (แตะ `freight_quotes.json` ชั่วคราว)
ถ้าต้องการอ่านอย่างเดียวจริง ๆ ให้รันแค่ `freight-ui` กับ `freight-review`

---

## รอบที่ผ่านมา

### 2026-09-08 — รีวิวงาน "เลื่อนสถานะอัตโนมัติจาก ETS" (ตัวรีวิว: Hermes · 6 finding)

ตรวจงานที่ยังไม่ commit ใน working tree ของ `main` (`lib/shipment-rules.cjs` + แก้ `api-server.js`,
`scan-shipment-docs.mjs`, `import-export-os.html`) · **4 จริง · 1 ไม่ใช่บั๊ก · 1 จริงแต่จัดชั้นเกิน**
· และ Claude เจอเพิ่ม **2 ตัวที่ร้ายแรงกว่าทุกข้อที่ถูกรายงาน**

**⚠ การอ้างอิงของ Hermes เชื่อตรงๆ ไม่ได้** — อ้าง `lib/shipment-status.mjs:22–41` (ไม่มีไฟล์นี้),
`hasCompleteBlData()` และ `actualArrivalSource` (ไม่พบทั้ง repo) · สาระตรง แต่ชื่อไฟล์/ฟังก์ชัน/เลขบรรทัด
สร้างขึ้นเอง → **ต้องตรวจทุกข้อกับโค้ดจริงก่อนเสมอ**

| # | finding | ผล | หลักฐาน |
|---|---|---|---|
| 1 | API ตอบ 2xx ไม่ได้แปลว่าเขียนสำเร็จ — scanner ทิ้ง response แล้ว mark ไฟล์เป็น processed | ✅ **จริง และหนักกว่าที่รายงาน** | server ตอบ `{ok,applied,total,rejected}` แต่ scanner log "สำเร็จ" ทันที (`scan:1149`) · เส้น `idMismatch` (`api-server:1877`) `return` เงียบ — ไม่เข้า `rejected` ไม่นับ `applied` ตอบ 200 · `saveSeen()` (`scan:1164`) อยู่**นอก** try/catch → โฟลเดอร์ถูก mark แม้ upsert พังทุกใบ |
| 2 | `actualArrivalDate` ที่กรอกมือ trigger สถานะอัตโนมัติได้ | ⚠️ **สาระจริง แต่จัดชั้นเกิน** | `sanitizeOrigin()` ไม่จำกัดค่า → ปลอม `_origin:'scan:x'` ได้จริง · **แต่** endpoint ผูก `127.0.0.1` ไม่มี auth บน loopback อยู่แล้ว ใครยิงได้ก็ตั้ง `stage` ตรงๆ ได้ตั้งแต่แรก → เป็นปัญหา **provenance** ไม่ใช่ security escalation |
| 3 | Air/courier ไม่ผ่านกติกาความครบ เพราะบังคับ vessel/voyage ทุกใบ | ✅ **จริง และหนักกว่าที่รายงาน** | ไม่ใช่แค่ "เข้า automation ไม่ได้" — `autoStage` คืน `'po'` แล้ว**เขียนทับสถานะจริง** (ดูบั๊ก A) · เจอ `mode=courier` โดน `received → po` 1 ใบ |
| 4 | ETS session ที่เสียถูกใช้ซ้ำทั้งรอบ | ✅ **จริง** | `scan:1054` `if (/90 วินาที/.test(e.message))` ปิด session **เฉพาะ timeout** · selector/navigation/parse ล้มได้ error คนละข้อความ → `etsSession` ค้าง ถูกใช้ต่อทุกโฟลเดอร์ที่เหลือ |
| 5 | `shipment_runs` บันทึกก่อนยืนยัน upsert | ❌ **ไม่ใช่บั๊ก — เจตนาที่บันทึกไว้แล้ว** | คอมเมนต์ `scan:1086` เขียนตรงๆ ว่า *"เก็บทุกใบไม่ว่าจะเขียนลงการ์ดได้หรือไม่"* · ตรงกับ `CLAUDE.md`: การ์ด 1 ใบเก็บได้ชิปเม้นเดียว ข้อมูลชิปเม้นอื่นต้องไม่หาย · เปลี่ยนเป็น persist-after-confirm = **ทำลายจุดประสงค์เดิม** (ข้อเสนอเพิ่มฟิลด์ status เป็นของดี แต่เป็นส่วนเสริม) |
| 6 | เทสยังไม่ครอบคลุม | ✅ **จริง** | `tests/lib/shipment-rules.cjs` มี 7 assert ใน 15 บรรทัด แต่**พิมพ์ว่า `6/6 passed`** (นับผิดเอง) · ไม่มีเทสฝั่ง server เลย ซึ่งเป็นที่ที่บั๊กจริงอยู่ · วางผิดที่ด้วย (`tests/lib/` เป็นที่เก็บ helper เช่น `pallet-optimum.mjs`) |

#### 🔴 บั๊ก A (Claude เจอ) — การ์ด 24 ใบถูก "ถอยสถานะ" · ละเมิดกฎข้อ 3

เขียนเทสให้แดงก่อนตามกติกา → **แดงจริง**:

```
stage ที่ autoStage คืนได้: etd, arrived, customs
STAGES ที่ระบบมี:         po, etd, arrived, customs, received
record ทั้งหมด: 1073 · มี _stageManual: 0
>>> การ์ดที่จะถูกถอยสถานะถ้า scanner แตะ: 24 ใบ
    received → etd = 12 · customs → etd = 6 · received → po = 5 · customs → po = 1
```

สามอย่างประกอบกัน:
1. **`r.stage = autoStage(...)` อยู่ *หลัง* ตัวกรอง `_fillEmptyOnly`** (`api-server:1849–1892`)
   → เกราะ "เติมเฉพาะช่องว่าง" ที่คุ้มทุกฟิลด์อื่น **ไม่คุ้ม `stage` เลย**
2. **`_stageManual` = 0 จาก 1,073 record** — เกราะที่โค้ดใหม่พึ่งพา ไม่มีในข้อมูลเดิมสักใบ
   (frontend เพิ่งเริ่มติดธงให้เฉพาะการแก้ครั้งใหม่)
3. **`autoStage` คืน `'received'` ไม่ได้เลย** — ไม่มีทางรักษาสถานะปลายทางไว้ได้

`received` = สินค้าถึงคลังแล้ว = สถานะสุดท้าย · ขัดกับ `CLAUDE.md` ที่บันทึกว่า
**"สถานะจาก Odoo เป็นพื้น ไม่ใช่เพดาน"** และขัดกฎข้อ 3 ท้ายไฟล์นี้ตรงๆ

> **ยังไม่ลงมือทำลายข้อมูล** — server ที่รันอยู่เริ่ม 10:03:38 ก่อนไฟล์ถูกแก้ (11:14)
> จึงยังใช้โค้ดเก่า · audit วันนั้นแตะ `stage` = 0 ครั้ง · **เป็นระเบิดเวลา** จะทำงานทันทีที่ restart

#### 🔴 บั๊ก B (Claude เจอ) — ทุกโฟลเดอร์ upsert แม้สกัดข้อมูลไม่ได้เลย

`fields.documentReceivedAt` ถูกตั้งทันทีหลังสร้าง `fields` ว่าง → `Object.keys(fields).length`
ไม่มีวันเป็น 0 → สาขา `else { log('ไม่พบข้อมูลใหม่ที่ดึงได้เลย') }` **ตายสนิท**

#### 🔴 บั๊ก C — เทสที่เขียนใหม่จับได้เอง: `isoDate()` รับวันที่ที่ไม่มีอยู่จริง

`/^\d{4}-\d{2}-\d{2}$/` ล้วนรับ `2026-13-45` ผ่าน → `addDays()` ได้ Invalid Date →
`toISOString()` โยน RangeError → **upsert ทั้งคำขอพังเป็น 400** · เป็นบั๊กชนิดเดียวกับที่
`parseFlexibleDate` เคยพลาดและแก้ไปแล้ว (บันทึกใน `CLAUDE.md`) แต่โมดูลใหม่พาเข้ามาอีก

#### ที่แก้ (commit นี้)

- **`nextStage(current, proposed)`** ใน `lib/shipment-rules.cjs` = เกราะเดียวที่แก้บั๊ก A ครบทั้ง 3 สาเหตุ:
  **สถานะอัตโนมัติเลื่อนได้เฉพาะไปข้างหน้า** เท่าเดิมหรือถอยหลัง = คืน `null` แล้วผู้เรียก **ไม่แตะฟิลด์เลย**
  · ไม่ต้องพึ่ง `_stageManual` ในทิศถอยหลังอีก (`_stageManual` ยังกันทิศไปข้างหน้าตามเจตนาเดิม)
- **แปลงชื่อสถานะรุ่นเก่าก่อนเทียบ** (`canonStage`: `booking`/`transit` → `etd`) ไม่งั้นสถานะเก่าจะเป็น
  "ไม่รู้จัก" แล้วหลุดเกราะ · สถานะที่ไม่รู้จักจริง (เช่นบอร์ด export) = ไม่แตะ
- **`CORE_FIELDS_AIR`** — air/courier ไม่บังคับ `vessel`/`voyage` (แก้ finding 3 ที่ต้นเหตุ ไม่ใช่แค่กันอาการ)
- **แก้ `changed` ให้ตรงกับที่เขียนจริง** — `changed` คำนวณก่อนบล็อกนี้ การเลื่อนสถานะจึงเคย
  **ไม่ขึ้น audit log และไม่ได้ stamp `_src`** = เปลี่ยนสถานะโดยไม่มีที่มา
- **`isoDate()` ตรวจว่าเป็นวันที่ที่มีอยู่จริง** (เทียบกลับหลังแปลง → กัน `2026-02-30` ที่ JS เลื่อนเอง)
- **`documentReceivedAt` ติดเฉพาะเมื่อสกัดได้จริง** อย่างน้อย 1 ฟิลด์
- **ย้ายเทสไป `tests/shipment-stage.cjs`** (25 assert) — ยก 7 assert เดิมมาครบ ไม่ทิ้งของใคร

#### ที่ยัง **ไม่แก้** ในรอบนี้ (จริงทั้งคู่ · เป็นบั๊กเดิม ไม่ได้เกิดจากงานชุดนี้)

| finding | ทำไมแยกออกมา |
|---|---|
| 1 · scanner ไม่ตรวจ `ok`/`applied`/`rejected` + `saveSeen` รันแม้ upsert พัง | เป็นบั๊กเดิมของ `scan-shipment-docs.mjs` มาก่อนงานชุดนี้ · การแก้ต้องออกแบบ ledger ใหม่ (retry/สถานะต่อโฟลเดอร์) = งานคนละก้อน |
| 4 · ETS session เสียถูกใช้ซ้ำ | บั๊กเดิมของ `lib/ets-lookup.mjs` เส้นทาง error · แก้ต้องแยกชนิด error + กำหนดเพดาน retry |

---

### 2026-09-04 — Codex รอบแรก (4 finding)
ตรวจ `e2b3b50` + `b3d16a8` · **แดง 4/4 ตอนเขียนเทส = จริงทั้งหมด ไม่มี false positive** · แก้ที่ `a82131e`

| # | finding | ผล | เทสที่พิสูจน์ |
|---|---|---|---|
| 1 | `switchType()` ไม่นับแถวที่เพิ่มเอง/หัวเอกสารว่าเป็นข้อมูล → สลับแม่แบบแล้วหายเงียบ | ✅ จริง | `freight-review` ข้อ 1 |
| 2 | ลบแถวที่เพิ่มเองไม่ถาม ไม่มี undo | ✅ จริง | `freight-review` ข้อ 2 |
| 3 | `normalizeQuote()` ตัด `prices` ที่เกินคอลัมน์เงียบ | ✅ จริง | `freight-review` ข้อ 3 |
| 4 | ไฟล์ Excel ที่ส่งออกไม่มีคำเตือนที่ขึ้นบนหน้าจอ | ✅ จริง | `freight-review` ข้อ 4 |

---

## ตรวจแล้ว — ไม่ใช่บั๊ก อย่ารายงานซ้ำ

รายการนี้คือสิ่งที่ **ดูเหมือนบั๊กแต่เป็นการตัดสินใจที่ตั้งใจแล้ว** พร้อมเหตุผล
ถ้าจะแย้ง ต้องมีหลักฐานใหม่ที่ขัดกับเหตุผลเดิม ไม่ใช่ชี้ซ้ำ

| เรื่อง | ทำไมถึงเป็นแบบนั้น |
|---|---|
| แม่แบบมี `CIS` แต่ไม่มี `CIC` | ทดสอบแล้วโมเดลแมป CIC เข้า CIS ได้ถูก · น่าจะเป็นค่าเดียวกัน (Container Imbalance) ถ้าเพิ่มจะได้สองแถวที่หมายถึงของเดียวกัน แล้วเสี่ยงกรอกซ้ำนับเงินสองรอบ |
| คำเตือน "ยังกรอกไม่ครบ" ขึ้นทั้งที่ LCL กับ FCL มีรายการไม่เท่ากันตามธรรมชาติ | เป็น false positive ที่ยอมรับ — เตือนเฉพาะทิศทางที่อันตราย (ตัวที่ **ถูกสุด** กรอกน้อยกว่า) และมีประโยคบอกว่าถ้าช่องว่างคือรายการที่ไม่มีค่าใช้จ่ายจริง ข้ามได้ |
| ขั้นบันไดใช้ได้แค่แถวค่าระวางหลัก | ในใบเสนอราคาจริง 18 ใบ มีแต่ค่าระวางที่คิดเป็นขั้น · ทำทุกแถวจะเพิ่ม UI ที่ไม่มีใครใช้ |
| `/RT` คิดเป็น CBM ล้วน ไม่ใช่ revenue ton | รู้อยู่และบันทึกไว้ใน `CLAUDE.md` แล้ว · ยังไม่แก้เพราะต้องตัดสินใจเชิงธุรกิจก่อนว่าจะคิด max(CBM, ตัน) เมื่อไหร่ |
| แถวมาตรฐานลบไม่ได้ (ปุ่ม × มีเฉพาะแถวที่เพิ่มเอง) | ตั้งใจ — AOF เคยลบ `O/F (Ocean Freight)` กับ `EXW Charge` หายถาวรมาแล้ว |
| ห้ามใช้ `--color-brand` (emerald) เป็นสีตัวอักษรหรือเส้นขอบบนพื้นสว่าง | วัดแล้ว contrast = **2.00** ตกทั้งเกณฑ์ข้อความ (4.5) และกราฟิก (3.0) · ใช้ได้แค่พื้นปุ่ม CTA / จุด accent |
| จุดสีบอกสถานะ shipment 6 ขั้นยังเป็นสี ทั้งที่ธีมบอก "เขียวเป็นสีเดียวในหน้า" | เบี่ยงจาก spec โดยตั้งใจ — มันเข้ารหัสสถานะงานที่ผู้ใช้อ่านด้วยสายตา ไม่ใช่การตกแต่ง |
| std packer นับชั้นจาก `Object.keys(zSeen).length` | รู้ว่าอาจ under-fill · **จงใจไม่แก้** เพราะแก้ heuristic แกนกลางเสี่ยงกลายเป็น over-report ซึ่งอันตรายกว่า |
| ค่าระวางหลักที่อ่านจาก PDF ไม่ถูกกรอกให้อัตโนมัติเสมอ | วัดแล้วอ่านได้แค่ 59% ของใบ · ตั้งใจให้คนกรอกเอง และมีแถบเตือนย้ำในหน้าตรวจผล |
| `shipment_runs.jsonl` เขียน**ก่อน**ยืนยันว่า upsert สำเร็จ | ตั้งใจ — การ์ด 1 ใบเก็บได้ชิปเม้นเดียว แต่โฟลเดอร์เดียวมีได้หลายชิปเม้น · ใบที่ upsert ปฏิเสธ **ต้องยังหาเจอ** เพื่อเอาไปสร้างการ์ดใหม่ ถ้าเขียนหลังยืนยันจะหายทันที (คอมเมนต์ `scan-shipment-docs.mjs:1086`) |
| `autoStage` ไม่มีทางคืน `'received'` | ตั้งใจ — "รับเข้าคลัง" ยืนยันด้วยเอกสารขาเข้าไม่ได้ ต้องมีคนกดเอง · เกราะที่ทำให้ปลอดภัยคือ `nextStage()` ที่ห้ามถอยหลัง ไม่ใช่การให้ระบบเดาสถานะปลายทาง |

---

## กติกาที่ห้ามถอด (ถ้า finding ไหนขัดกับข้อเหล่านี้ ให้ยกมาคุยก่อน)

1. **ห้ามตัดข้อมูลโดยไม่บอก** — LIMIT / slice / cap ทุกจุดต้องแจ้งผู้ใช้เมื่อตัดของจริง
2. **รายงานเกินจริงอันตรายกว่ารายงานต่ำ** — เมื่อไม่แน่ใจ ให้เลือกทางที่รายงานต่ำกว่า
3. **ห้ามทับค่าที่ผู้ใช้กรอกเอง** — เติมได้เฉพาะช่องว่าง
4. **ห้าม re-render DOM ระหว่างผู้ใช้พิมพ์** — อัปเดตเฉพาะเซลล์ยอดเงินแบบ in-place
5. **คำเตือนต้องตามไปทุกทางที่ตัวเลขออกจากระบบ** — หน้าจอ ไฟล์ Excel และการพิมพ์
